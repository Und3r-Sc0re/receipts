# dependencies
node_modules/
.pnp
.pnp.js

# next.js
web/.next/
web/out/
web/build/

# extension build output — dist/ IS committed on purpose so judges can
# load the extension with no build step (git clone -> Load unpacked).
# Rebuild with `npm run build` in extension/ after any src/ change.

# env — never commit real keys
.env
.env.local
.env*.local
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
